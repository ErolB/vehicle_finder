# --- Secrets ---
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- Generated pipeline artifacts (tracked) ---
# model/      : fine-tuned weights from finetune.py; model.safetensors (~400 MB)
#               is stored via Git LFS (see .gitattributes).
# embeddings/ : vectors from embedding.py.
# html/       : raw pages from scraper.py.


# Smaller, but still regenerable from the pipeline. Uncomment to stop
# tracking them (descriptions/ is produced by an LLM, so costs money to
# rebuild — left tracked by default).
# text/
# descriptions/

# --- Editor / OS ---
.vscode/
.idea/
Thumbs.db
Desktop.ini
*.swp
